version: "2"
services:
  app:
    container_name: poketeam
    restart: always
    build: .
    ports:
      - "5000:5000"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./docker-data/db:/data/db
    ports:
      - "27017:27017"